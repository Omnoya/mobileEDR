{# src/EDR/AppliBundle/Resources/views/layout.html.twig #}

{% extends "::base.html.twig" %}

{% block title %}
    {{ parent() }}
{% endblock %}

{% block menu %}
    {{ parent() }}
    {#<ul class="nav nav-pills nav-stacked">
        <li><a href="{{ path('fos_user_security_login') }}">Connexion</a></li>
        {% for categorie in categories %}
            #}{# utilisation de la fonction {{ path() }} pour faire des liens vers d'autres routes #}{#
            <li><a href="{{ path('etab_category_show', {'category' : categorie.nom }) }}">{{ categorie.nom }}</a></li>
        {% endfor %}
    </ul>#}

    {#<nav>
        <div class="nav-wrapper">
            <a href="#" class="brand-logo">EDR Application</a>
            <ul id="nav-mobile" class="right hide-on-med-and-down">
                {% for categorie in categories %}
                    <li>
                        <a href="{{ path('etab_category_show', {'category' : categorie.nom }) }}">{{ categorie.nom }}</a>
                    </li>
                {% endfor %}
                <li><a href="{{ path('fos_user_security_login') }}">Connexion</a></li>
            </ul>
        </div>
    </nav>#}

    <nav>
        <div class="nav-wrapper">
            <a href="#" class="brand-logo">EDR Application</a>
            <a href="#" data-activates="mobile-demo" class="button-collapse"><i class="material-icons">menu</i></a>
            <ul class="right hide-on-med-and-down">

                    {% for categorie in categories %}
                        <li>
                            <a href="{{ path('etab_category_show', {'category' : categorie.nom }) }}">{{ categorie.nom }}</a>
                        </li>
                    {% endfor %}

                <li><a href="{{ path('fos_user_security_login') }}"><i class="material-icons right">face</i></a></li>
            </ul>
            <ul class="side-nav" id="mobile-demo">

                    {% for categorie in categories %}
                        <li>
                            <a href="{{ path('etab_category_show', {'category' : categorie.nom }) }}">{{ categorie.nom }}</a>
                        </li>
                    {% endfor %}

                <li><a href="{{ path('fos_user_security_login') }}">Connexion</a></li>
            </ul>
        </div>
    </nav>

{% endblock %}

{% block body %}

    {# On définit un nouveau bloc, que les vues du bundle pourront remplir #}
    {% block edrappli_body %}

    {% endblock %}

{% endblock %}

{% block javascripts %}
    {{ parent() }}

    {#<script src="http://code.jquery.com/jquery-1.11.0.min.js"></script>#}
    <script> $(".button-collapse").sideNav(); </script>
    {#<script type="text/javascript" src="{{ asset('bundles/edrappli/js/googlemap.js') }}"></script>#}

{% endblock %}