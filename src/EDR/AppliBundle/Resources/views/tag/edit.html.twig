{#ADMIN#}

{#{% extends 'base.html.twig' %} #}
{% extends "EDRAppliBundle::layoutadmin.html.twig" %}

{% block body %}
<h3 class="admin_h3">Modifier un tag</h3>

    <div class="row">
        <div class="col s12 admin_form">
            {{ form_widget(edit_form) }}
            {{ form_start(edit_form) }}
            <div class="admin_nav">
                <input type="submit" class="admin_edit" value="Modifier"/>
                <a href="{{ path('tag_index') }}" class="admin_retour">&crarr;</a>
            </div>
            {{ form_end(edit_form) }}
        </div>
    </div>
    {#<ul>
        <li>
            {{ form_start(delete_form) }}
            <button type="submit" class="btn btn-danger">
                <i class="glyphicon glyphicon-trash"></i>
                Supprimer
            </button>
            {{ form_end(delete_form) }}

        </li>
    </ul>#}
{% endblock %}
