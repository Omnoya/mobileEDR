{#ADMIN#}

{#{% extends 'base.html.twig' %} #}
{% extends "EDRAppliBundle::layoutadmin.html.twig" %}

{% block body %}
<h3 class="admin_h3">Modifier un tag</h3>



    {{ form_start(edit_form) }}
        <div class="admin_form">{{ form_widget(edit_form) }}

                <button type="submit" class="admin_submit">
                    Modifier
                </button> <button class="admin_submit"><a href="{{ path('tag_index') }}" >
        Retour à la liste
    </a></button>
    {{ form_end(edit_form) }}</div>

    {#<ul>
        <li>
            {{ form_start(delete_form) }}
            <button type="submit" class="btn btn-danger">
                <i class="glyphicon glyphicon-trash"></i>
                Supprimer
            </button>
            {{ form_end(delete_form) }}

        </li>
    </ul>#}
{% endblock %}
